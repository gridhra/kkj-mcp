{
  "name": "kkj-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for Japanese Government Procurement Portal API (官公需情報ポータルサイトAPI)",
  "type": "module",
  "bin": {
    "kkj-mcp": "./build/index.js"
  },
  "scripts": {
    "generate:knowledge": "tsx scripts/generate-knowledge-data.ts",
    "build": "npm run generate:knowledge && tsc && chmod +x build/index.js",
    "build:workers": "npm run generate:knowledge && tsc",
    "watch": "tsc --watch",
    "start": "node build/index.js",
    "dev:workers": "wrangler dev",
    "deploy:workers": "wrangler deploy",
    "inspector": "npx @modelcontextprotocol/inspector node build/index.js",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --coverage",
    "test:watch": "vitest --watch"
  },
  "files": [
    "build"
  ],
  "keywords": [
    "mcp",
    "japanese-government",
    "procurement",
    "api",
    "official-procurement"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.1",
    "fast-xml-parser": "^5.3.2",
    "hono": "^4.6.14",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "@types/node": "^25.0.3",
    "@vitest/coverage-v8": "^4.0.16",
    "@vitest/ui": "^4.0.16",
    "tsx": "^4.19.2",
    "typescript": "^5.7.0",
    "vitest": "^4.0.16",
    "wrangler": "^4.54.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
