# サーバーモード (stdio または http)
SERVER_MODE=http

# Node環境 (development または production)
NODE_ENV=development

# HTTPサーバー設定 (SERVER_MODE=httpの場合のみ)
PORT=3000
HOST=0.0.0.0

# CORS設定
CORS_ORIGIN=*
CORS_CREDENTIALS=false

# APIキー認証（カンマ区切りで複数設定可能）
# 本番環境でのみ有効化されます
# 開発環境ではlocalhostへのアクセスは認証不要です
API_KEYS=your-api-key-1,your-api-key-2,your-api-key-3

# 使用例:
# curl -H "Authorization: Bearer your-api-key-1" http://localhost:3000/mcp/sse
